version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: development
    ports:
      - "5000:5000"
    volumes:
      - ./app:/app/app
      - ./tests:/app/tests
      - ./scripts:/app/scripts
      - ./run.py:/app/run.py
      - ./asml-link.db:/app/asml-link.db
    env_file:
      - .env
    environment:
      - DEBUG=true
      - DATABASE_URL=sqlite+aiosqlite:///./asml-link.db
    command: uv run gunicorn app.main:app -k asgi --bind 0.0.0.0:5000 --reload
    restart: unless-stopped
